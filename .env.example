# Copy this file to `.env` and update the values for your environment.
# Only the required variables (plus the model override) are included here.
# Optional tuning/testing flags are documented in README.md.

DATABASE_URL="postgresql://vbt:vbt@postgres:5432/vbt_hiring_test?schema=public"
OPENAI_API_KEY="your-openai-key"
OPENAI_BASE_URL=https://api.openai.com/v1
OPENAI_MODEL="gpt-5-mini-2025-08-07"
