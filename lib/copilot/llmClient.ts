/**
 * Wrapper around GPT-5.1 (or other LLM) calls.
 * Implementation will be added after data models and workflows exist.
 */
export async function generateWithCopilot(prompt: string) {
  console.info("Copilot prompt placeholder:", prompt);
  return { content: "LLM response placeholder." };
}

export default generateWithCopilot;

